name: ${name}
main: ${main}
version: ${version}
author: ${author}
api-version: 1.20.4
folia-supported: true
loader: ${loader}

dependencies:
  server:
    packetevents:
      load: BEFORE
      required: true
      join-classpath: true
